# Pinned runtime deps for pip-based installs / Docker images (Phase 0 scaffold).
# Mirror of pyproject [project.dependencies]. Heavy AI deps added in Phase 2/3.
fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.0
loguru>=0.7.2
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
psycopg[binary]>=3.2.9
